# Tilefin custom ujust recipes
#
# This file is the sole entry point imported by the upstream
# ublue-os-just justfile (import? "60-custom.just"). Individual
# recipe files are imported below so each domain stays in its
# own file.

import? '61-tilefin.just'
import? '62-bmd.just'
